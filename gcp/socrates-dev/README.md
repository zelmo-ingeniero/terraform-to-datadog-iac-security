
# GCP Socrates

## Authorization

Install the `gcloud` command and login to the desired project. The terraform provider to GCP can just interact with only 1 project at time

```bash
gcloud auth application-default login --no-launch-browser
```
















